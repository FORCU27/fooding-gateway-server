spring.application.name: gateway

server:
  port: 8081

spring:
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: target-service
              uri: http://localhost:8082
              predicates:
                - Path=/**
